<%= form_for(@product_feature) do |f| %>
  <% if @product_feature.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_feature.errors.count, "error") %> prohibited this product_feature from being saved:</h2>

      <ul>
      <% @product_feature.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :feature_id %><br>
    <%= f.number_field :feature_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
