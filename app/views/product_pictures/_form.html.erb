<%= form_for(@product_picture) do |f| %>
  <% if @product_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_picture.errors.count, "error") %> prohibited this product_picture from being saved:</h2>

      <ul>
      <% @product_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :picture_id %><br>
    <%= f.number_field :picture_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
