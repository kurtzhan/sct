<%= form_for(@material_item) do |f| %>
  <% if @material_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@material_item.errors.count, "error") %> prohibited this material_item from being saved:</h2>

      <ul>
      <% @material_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :material_id %><br>
    <%= f.number_field :material_id %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
